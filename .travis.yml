sudo: required
language: python

install:
  - pip install whaledo

services:
  - docker

env:
  global:
    - MAKEFLAGS="-j 2"
  matrix:
    - TEST_SECTION=1
    - TEST_SECTION=2

script: "./bin/make clean test-${TEST_SECTION}"

notifications:
  email:
    on_success: change # default: change
    on_failure: change # default: always
