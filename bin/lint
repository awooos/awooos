#!/usr/bin/env python

import sys
import glob
import whaledo

command = ["clang-check"]
command += glob.glob("./**/*.c", recursive=True)
command += ["--", "-I", "include", "-I", "src/libraries/dux-mm-i386/include"]

exit_code = whaledo.run("os-development-x86", command)
exit(exit_code)
