#!/usr/bin/env python

import sys
import whaledo

make_config_flags = 'AWOO_MAKE_CONFIG=config.mk.dist'
qemu_flags = "QEMU_FLAGS=-display none"

command = ["make", make_config_flags, qemu_flags]
command += sys.argv[1:]

whaledo.run("os-development-x86", command)
