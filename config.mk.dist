# Use ?= for TARGET, so it can be overridden on the command line.
TARGET  ?= i386

CC      := clang
AS      := nasm
AR      := ar
RANLIB  := ranlib
LD      := ld

BOCHS   := bochs

# NOTE: xorriso, mkisofs, _and_ genisoimage may all have different defaults.
MKISOFS := xorriso -as mkisofs
